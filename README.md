# Y. L. Tank Game

This is the original project entry README for the Y. L. Tank Game workspace.

The folder has been reorganized, but the project source is still kept locally:

- Main bot source: `src/bots/xjy.py`
- 1v1 bot source: `src/bots/xjy1v1.py`
- Demo bots: `src/bots/demo/`
- Historical source versions: `src/archive_versions/`
- Final runtime files: `bin/`
- Local archives and old build outputs: `archive/`
- Slides and screenshots: `docs/`

For the newly written portfolio-style project description, see:

- `PROJECT_README.md`

Note: the pre-organization README content was overwritten before Git history existed in this folder, so there is no exact local backup to restore from. This file is intentionally kept minimal so it does not replace the original project documentation with the new portfolio write-up.
